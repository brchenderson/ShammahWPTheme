<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
            <a class="navbar-brand" href="{{ishome ? '#' : site.url}}"><span class="pull-left" id="brand-text">Shammah<br>Health<br>Center</span><img src="{{function('get_header_image') }}" class="pull-right"/></a>
              <div class="clearfix"></div>

    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav" id="main-navigation">      
        {% for item in menu.get_items %}                
          <li><a href="{{ishome ? '#' ~ item.slug : site.url ~ '/#' ~item.slug }}" class="navlink">{{item.title}}</a></li>                 
        {% endfor %}
      </ul>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="{{ishome ? '#donate-now' : site.url ~ "/#donate-now" }}" class="btn btn-default donate-button">Donate Now</a></li>        
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>




